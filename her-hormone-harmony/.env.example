# No environment variables are required right now. The free guide and
# the Handbook both link straight to their Gumroad pages (see
# lib/constants.ts), and the contact form just logs submissions.
#
# If you later wire the contact form (app/api/contact/route.ts) up to a
# real email service such as Resend or Postmark, add its API key here
# and in Vercel: Project Settings -> Environment Variables.
